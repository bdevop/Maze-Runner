apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: maze-runner
  labels:
    app.kubernetes.io/name: maze-runner
    app.kubernetes.io/part-of: maze-runner-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: maze-runner
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80